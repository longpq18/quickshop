<%= form_for(@testimonial) do |f| %>
  <% if @testimonial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@testimonial.errors.count, "error") %> prohibited this testimonial from being saved:</h2>

      <ul>
      <% @testimonial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <p><label>Name</label><%= f.text_field :name %></p>
    <p><label>Regency</label><%= f.text_field :regency %></p>
    <p><label>Website</label><%= f.text_field :website %></p>
    <p><label>Content</label><%= f.cktext_area :content %></p>
    <p><label>Image</label><%= f.file_field :image %></p>
    <%= f.submit 'Create Testimonial', :class => 'register' %>
  </div>
<% end %>
